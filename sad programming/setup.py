from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='this is an example project to showoff how hard the BA students are',
    author='Evelyn Guo',
    license='MIT',
)
